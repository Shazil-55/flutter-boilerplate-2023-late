enum ForgetPasswordMarathon { email, phone, opt, reset, congrats }

enum SignUpMarathon {
  phone,
  otp,
  credentials,
  basicInfo,
  findRestaurants,
  visitingTime,
  visitReason,
  accommodation,
  visitWith,
  congrats
}

enum UserType { visiting, living }

enum LoadingState { none, loading, error, succeed }

enum TypeToast { normal, error, success }
