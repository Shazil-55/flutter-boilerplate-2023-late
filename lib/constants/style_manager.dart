import 'package:flutter/cupertino.dart';

import '../application/theme/app_colors.dart';

BoxShadow shadow = const BoxShadow(
    color: AppColor.kUnselectedColor,
    spreadRadius: 5,
    blurRadius: 7,
    offset: Offset(
      0,
      3,
    ));
